import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    icon: {
        fontSize: 32,
    },
    container: {
        margin: 20,
        color: '#333',
        flexDirection:'row', 
        flexWrap:'wrap'
    },
    title: {
        fontSize: 24,
        fontWeight: '600'
    }
});