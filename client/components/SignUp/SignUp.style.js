import { StyleSheet } from 'react-native'

const style={
    noteContainer: {
        marginTop: 10,
        display: 'flex',
        flexDirection: 'row'
    }
}

export default StyleSheet.create(style);