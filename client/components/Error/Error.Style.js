import { StyleSheet } from 'react-native';

const styles = {
    container: {
        borderRadius: 5,
        backgroundColor: '#333',
        marginTop: 10,
        marginBottom: 10,
    },
    message: {
        color: '#fff',
        width: '100%',
        padding: 10,
        fontWeight: '600',
    }
}

export default StyleSheet.create(styles);