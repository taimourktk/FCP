import { StyleSheet } from 'react-native'

const styles = {
    container: {
        width: '100%',
        height: '100%',
        backgroundColor: 'white',
    },
    teamName: {
        fontWeight: '600',
        color: '#333',
        fontSize: 18,
        marginTop: 5,
    },
    memberCount: {
        color: '#333',
        marginTop: 5,
    }
}

export default StyleSheet.create(styles);